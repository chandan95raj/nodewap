import Home from "../cmp/home";
import Layout from "../cmp/layout";

const Index =()=>{
  const design = (
      <>
         <Layout>
            <Home />
          </Layout> 
      </>
  );
  return design;
}

export default Index;